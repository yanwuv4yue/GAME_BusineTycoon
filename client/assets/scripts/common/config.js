let config = {
    // game
    MAX_PLAYER_COUNT: 6,
    ROOM_MAX_PLAYER_COUNT: 5,
    ROOM_MIN_PLAYER_COUNT: 2,

    CHESS_BOARD_SIZE: 10,

    // debug
    isDebug: true,
};

module.exports = config;